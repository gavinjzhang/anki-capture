# API base (leave empty to use same origin)
# For local dev Worker:
# VITE_API_BASE=http://localhost:8787
# For deployed Worker:
# VITE_API_BASE=https://anki-capture-api.<account>.workers.dev

# Clerk
# Required for auth-enabled builds
# VITE_CLERK_PUBLISHABLE_KEY=pk_live_...
